# 17 DNS 流量隐写入门

## 题目目标

给你一个 `pcap` 文件。真正有用的信息不在 HTTP，也不在图片，而是藏在 DNS 查询名里。

## 附件

- `files/dns-bursts.pcap`

## 预期收获

- 学会从流量中提取 DNS query
- 学会把子域名片段重新拼接
- 学会判断拼出来的内容是不是编码层

## 建议限制

- 不要盲目跟所有协议
- 先只盯着 DNS
